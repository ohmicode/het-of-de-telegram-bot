{
  "name": "het-of-de-telegram-bot",
  "version": "1.0.0",
  "description": "A Telegram bot to learn Dutch articles 'de' and 'het'",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js | pino-pretty",
    "dev": "node src/index.js | pino-pretty",
    "build": "esbuild src/index.js --bundle --platform=node --target=node20 --outfile=dist/index.js --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "telegram",
    "bot",
    "dutch",
    "het",
    "de"
  ],
  "author": "ohmicode",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/firestore": "^7.7.0",
    "@google-cloud/secret-manager": "^5.5.0",
    "axios": "^1.7.2",
    "fastify": "^5.7.4"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "pino-pretty": "^11.0.0"
  }
}
